# After

Change:

- Added a repeatable Python report script.
- Added sample inputs.
- Added tests for totals, invalid rows, unknown customers, and stable CSV output.

Verification:

- `python3 -m unittest discover -s case-studies/python-report-automation/tests`
