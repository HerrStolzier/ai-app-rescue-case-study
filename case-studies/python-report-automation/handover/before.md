# Before

Expected behavior:

- Orders are turned into a stable customer-level report.
- Missing or malformed rows are visible instead of silently corrupting totals.

Actual behavior:

- Reporting depends on manual spreadsheet work.
- Invalid totals and unknown customers are easy to miss.
- There is no repeatable command.
