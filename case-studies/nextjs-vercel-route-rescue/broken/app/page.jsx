export default function Page() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 32 }}>
      <h1>Broken AI-built support app</h1>
      <p>The UI exists, but the expected API endpoint is missing.</p>
      <code>GET /api/chat</code>
    </main>
  );
}
