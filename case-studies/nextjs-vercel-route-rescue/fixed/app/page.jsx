export default function Page() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 32 }}>
      <h1>Fixed AI-built support app</h1>
      <p>The API endpoint now follows the App Router route convention.</p>
      <code>GET /api/chat</code>
    </main>
  );
}
